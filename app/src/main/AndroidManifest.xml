<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vcu.groupr.relicrepository">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.vcu.groupr.relicrepository.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Catalog"></activity>
        <activity android:name=".CatalogAdd"></activity>
        <activity android:name=".CatalogItem"></activity>
        <activity android:name=".CatalogEdit"></activity>
        <activity android:name=".Calendar"></activity>
        <activity android:name=".CalendarAdd"></activity>
        <activity android:name=".CalendarEvent"></activity>
        <activity android:name=".CalendarEdit"></activity>
    </application>

</manifest>